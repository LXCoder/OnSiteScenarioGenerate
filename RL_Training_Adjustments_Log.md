## 自动驾驶对抗场景生成 - 强化学习(PPO)背景车与奖励函数深度重构

**问题提出：**
1. **背景车缺乏攻击性与过拟合**：原奖励函数对距离使用单调的负奖励（`-1.0`），导致背景车仅仅学到了在一定距离外“尾随”或“逃逸”，而不主动寻求碰撞。且碰撞后未做终止截断，导致碰撞事件没有被强化为一个强烈的“终极目标”。
2. **多智能体与单智能体接口冲突（蜂群效应）**：在环境 `step` 时，PPO 虽然只能输出一个连续动作（accel, steer），但 `applyBgActions` 中通过循环把同一个动作强制赋给了所有的背景车，导致训练时所有车做完全一致的动作。
3. **PPO 转向控制失效（Steer 漂移与饱和）**：`steer` 动作原本在代码中被接收，但在推演物理位置时，强制使用了沿固定路径插值的 `_posOnPath`，使得 `steer`对物理环境毫无影响。PPO 发现改变方向盘无用后，在探索中导致权重偏移，使得输出长期卡在极值边界（如 `0.7` 或 `-0.7`）。
4. **异常驾驶行为（逆行与原地打转）**：开放真实的转向控制后，模型在初期会疯狂乱打方向盘，甚至掉头逆行或冲出路网。
5. **碰撞检测精度不足**：仅靠中心点距离判定碰撞导致在车辆交汇等场景下误判率极高。

**解决方案：**
1. **引入 Random Attacker (随机主攻手) 机制**：
   * 在 `doReset` 中，每回合随机从场景车辆中挑选一个背景车作为 `_attacker_name`。
   * `buildObs` 与 `computeReward` 仅从该“主攻手”的视角计算状态与奖励。
   * `applyBgActions` 中，只有主攻手接收 PPO 输出的 `(accel, steer)` 动作；其余背景车降级为 NPC，严格按照规划好的 JSON 轨迹匀速行驶。完美适配单智能体 PPO，并极大地增强了模型的空间攻击泛化能力。
2. **物理控制解耦（引入运动学自行车模型）**：
   * 移除对主攻手的 `_posOnPath` 硬绑定。
   * 引入 `wheelbase = 2.8`，通过 `yaw_rate = v * tan(steer) / L` 真实计算偏航角，并通过纯数学公式 `x += v*sin(heading)*dt` 结算坐标。方向盘动作终于对物理环境产生实际影响。
3. **高精度 OBB 碰撞检测**：
   * 废弃中心点测距，引入二维分离轴定理（SAT算法）实现的 `_check_bbox_collision`。根据两车的物理长宽（4.8m x 2.0m）和偏航角生成矩形边框，精确判定碰撞。
4. **重塑对抗型奖励函数 (Adversarial Reward Shaping)**：
   * **明确目标**：将碰撞奖励调整为 `+15.0`，并触发 `done = True` 截断回合，强化“一击脱离”的终极目标。
   * **引入“准星”机制 (追踪瞄准奖励)**：计算背景车头指向主车中心点的角度差 `heading_err_ego`。只要主车在前方 30 米内，视线越对准主车得分越高，极大加快了 `steer` 寻敌能力的收敛。
   * **分段博弈奖励**：利用前后距离（`forwardDist`）和速度差，精确惩罚“落后减速”和“超前后逃跑”行为，奖励“落后加速”和“超前后压车别车”等高级对抗策略。
5. **严厉约束非理性行为**：
   * **平滑惩罚**：增加绝对转向惩罚（`-abs(steer)*0.2`）以及极高的方向盘突变惩罚（`steer_diff * 10.0`），彻底消除高频蛇形走位（Bang-Bang Control）。
   * **车道越界惩罚**：调用 `LaneProjector`，当主攻手偏离所在车道理论中心超 2.5m 时，直接触发致命惩罚（`-50`）并终止回合。
   * **逆行惩罚**：比较车辆实际朝向与预期车道朝向，偏差超过 90 度判定为掉头/逆行，直接扣除 `-50` 并终止。
   * **恢复进度低保**：为了防止智能体在初期因严厉惩罚而学会“原地趴窝自杀”，增加了基于推进速度的基础收益（每步 `+0.2`），使其明白沿着路往前开是获得对抗大奖的先决条件。

---
# RL 奖励函数深度调优记录 (当前轮次总结)

本文档记录了本轮对话中，针对背景车（主攻手）强化学习模型出现的梯度不稳定、原地转圈以及“自杀式逃避惩罚”等核心问题的分析与解决过程。

## 0. 奖励值归一化与连续惩罚机制重构

**问题提出：**
1. **梯度不稳定**：原始奖励函数的数值跨度极大（如 +15 碰撞大奖与 -50 越界重罚），这种未归一化的稀疏大数值波动导致模型难以稳定收敛，Value Network 评估失效。
2. **原地转圈行为**：车辆的转向动作惩罚是线性的（同时缺乏对静态满打方向盘的有效抑制，`steer_diff` 此时为 0），导致车辆经常持续满打方向盘（如 0.7 rad）在原地打转，进行无效探索。
3. **“自杀式逃避惩罚”现象**：原环境中，若车辆长时间静止（速度 < 0.1 达 15 帧），环境会直接触发 `checkDone` 终止 Episode，但没有施加足够的终止惩罚。模型为了规避长期行驶带来的轻微动作扣分，学会了“只要停下来不跑，就能少扣分快速结束”的自杀式漏洞。

**解决方案：**
1. **全局奖励分级归一化**：
   * 将所有中间引导型奖励（进度、交互诱导、控制平滑度等）统一缩小一个量级至 `0.01 ~ 0.1` 范围。
   * 将核心成功/失败条件（如碰撞、严重越界、逆行）的单次截断奖励限制在合理区间（如最终使用 `[-3.0, 2.0]` 并通过 `np.clip` 强制兜底），确保每一步的 Reward 梯度平滑，明确引导信号。
2. **非线性（平方）转向惩罚**：
   * 针对方向盘绝对转角引入平方项惩罚：`r -= (steer ** 2) * 0.2`。这使得极限转向角的惩罚呈指数放大，逼迫模型在使用完大转角后迅速回正方向盘，有效抑制了持续打满方向盘原地画圈的问题。
3. **废除提前终止，改为“持续掉血”**：
   * **移除终止条件**：在 `checkDone` 阶段彻底移除了“卡死即重置”的逻辑，保证模型无法通过静止来逃避回合长度。
   * **引入连续重罚**：在 `computeReward` 中新增卡死连续惩罚机制。一旦车辆速度持续小于 0.1 超过阈值（15帧），每一帧都会被扣除大额分数（`-0.5`）。这迫使模型意识到，原地停滞带来的累积负收益远大于正常行驶时的微调惩罚，强制其必须动起来。
4. **修正动态远离逻辑**：
   * 完善了静态距离惩罚 `_reward_static_penalties`。不仅惩罚绝对距离过远，还结合物理速度差 `speedDiff` 与纵向距离 `forwardDist`，精准识别并惩罚“前车跑得快”或“后车跑得慢”导致的“越拉越远”消极博弈行为。

---

# RL 训练架构调整与弯道优化记录

本文档记录了在强化学习 (RL) 训练主车 (Ego) 过程中遇到的核心问题及其解决方案，便于后续回顾。

## 1. 训练目标与控制逻辑切换

**问题提出：**
原始项目架构的目的是训练背景车 (DQN/PPO) 来干扰主车 (由选手控制)。现在需要反转目标，改为训练主车 (Ego) 的避障与寻路能力。

**解决方案：**
1.  **控制权反转**：在 `MySimulator.py` 的 `updateEgo` 和 `_afterOneStepTraining` 中，取消调用选手的 `PlayerManager`，转而接收 RL 环境输出的连续动作 `(accel, steer)`。
2.  **观测对象切换**：将 `buildObs` 的主体从背景车修改为 Ego 车，使其雷达和导航信息基于 Ego 的位姿生成。
3.  **NPC 降级**：在训练 Ego 的阶段，将所有背景车降级为按预设 JSON 轨迹匀速行驶的 NPC (`applyBgActions` 使用路径跟随逻辑)。

## 2. 奖励值分项归一化优化

**问题提出：**
原先的奖励函数中，各项奖励的数值量级差异巨大（例如：距离车道中心的偏移惩罚可能只有 -0.1，而碰撞可能直接给 +2.0 或 -10.0）。这种未归一化的大数值波动会导致 PPO 算法中 Value Network（价值网络）的梯度爆炸，使得模型难以收敛，同时超参数（如 `vf_coef`）也会失效。此外，未分项归一化的直接累加会导致模型完全忽视权重较小的任务（产生极端行为）。

**解决方案：**
引入“分项归一化加权求和”的最佳实践。
1.  **分项映射到 [0, 1]**：将速度贴合度、车道偏移量、航向角偏差等每个物理指标，都通过线性衰减映射到严格的 `[0.0, 1.0]` 区间。
2.  **设定相对权重**：为各项归一化后的指标分配权重（如 `0.2`、`0.3`），确保单步最大总奖励严格等于 `1.0`。
3.  **好处**：这不仅让 PPO 训练极度稳定，而且通过改变权重比例，可以非常直观地调整车辆的驾驶风格（例如更偏向于速度还是更偏向于安全）。

## 3. 奖励机制重构：解决“原地苟活”现象与引入 Frenet 进度

**问题提出：**
在全正向奖励机制下，发现车辆在道路上缓慢行驶甚至原地不动，只要熬过 400 step 就能获得很高的累计生存奖励；反而如果尝试加速跑动，往往因为碰撞或出界导致回合提前结束，总分偏低。这导致模型偏向于“缓慢移动”甚至“原地趴窝”。同时，环境原本缺乏“终点”的概念。

**解决方案：**
将奖励函数由“按时间发工资”改为“**全正向计件工资**”模式，并引入基于 Frenet 坐标系的纵向进度计算。
1.  **Frenet 纵向进度提取 (`_getFrenetProgress`)**：编写专门算法，将车辆当前笛卡尔坐标投影到 `egoSmoothedPath`（平滑参考轨迹）上，实时算出车辆在路径上的纵向行驶里程 (`s_ego`)、路径总长 (`s_total`)，以及剩余距离。
2.  **废除生存低保**：移除了每帧固定给予的生存奖励。
3.  **激活条件限制**：规定只有在车速 `v > 1.0 m/s` 时，才发放车道居中和航向对齐奖励，杜绝原地白嫖分数。
4.  **单步推进量奖励 (Delta Progress)**：引入基于 Frenet 坐标系的单步实际前进距离 (`delta_s = s_ego - prev_s_ego`)。只有车辆真正在纵向上前进了，才能获得进度分数。
5.  **终点大奖**：当距离终点不足 5 米时，一次性给予 `+10.0` 的大额稀疏奖励，赋予模型强烈的冲线动机。

## 4. 动作映射修复：解决“训练神勇，推理拉垮”

**问题提出：**
模型训练完毕后，在推理模式下的表现极差，车辆经常猛打方向盘导致失控冲出车道。

**解决方案：**
排查发现是训练与推理的**动作映射 (Action Squash)** 不一致导致的致命 Bug。
在训练环境 (`TessngDrivingEnvPPO.py`) 中，模型输出的 `[-1.0, 1.0]` 动作被 `tanh` 函数映射并缩放为了真实的物理边界 (如最大转向角 0.7 rad)；而在推理环境 (`_afterOneStepInference`) 中，此前直接将原始输出赋值给了物理属性。
*   **修复**：在推理模式中，完美复刻训练时的 `tanh` 映射与缩放逻辑，并同步加入了防抖动的动作变化率限制 (Delta Clip)。

## 5. 场景动态适配：解决死板的轨迹依赖

**问题提出：**
`MySimulator.py` 中写死了一条 Ego 的参考路径 (`egoCenterLine`)。在多场景训练时，由于不同场景下 Ego 的途经点不同，写死的路径导致车辆立刻被判定出界。同时，不同场景下 Ego 的期望速度也不同，代码中却写死了 15.0 m/s。

**解决方案：**
1.  **动态路径加载**：在 `initBgAgents` 中，根据场景 JSON 文件，动态查找 `name="ego"` 的车辆信息。提取其 `path` 并进行平滑处理，作为当前场景下 Ego 的专属导航轨迹。
2.  **动态期望速度**：同步读取 JSON 中的 `speed` 字段，赋值给 `self._ego_target_speed`，并在奖励函数中以此作为计算速度偏差的锚点。

## 6. 弯道专项优化：解决入弯冲出车道问题

**问题提出：**
即使动作映射和轨迹加载都正确，车辆在直线表现尚可，但在弯道处完全不行，经常直挺挺地冲出车道，不知如何过弯。

**解决方案：**
核心原因是模型“缺乏内在的转向动机”且“感知与惩罚基准不一”。
1.  **统一基准（从车道中心切换为轨迹中心）**：废弃 TESSNG 原生但不稳定的 `LaneProjector`（在急弯处车道中心线提取往往不准确）。统一使用我们高精度计算的 Frenet 投影 (`_getFrenetProgress`) 所得出的**轨迹横向偏差 (lateral_dist)** 作为居中奖励的唯一依据，强迫模型死死咬住规划好的路径。
2.  **引入航向对齐奖励**：在 `_getFrenetProgress` 中增加计算当前路径切线的**预期航向角 (expected_heading)**。在奖励函数中，新增 `r_heading` 项。
    *   模型必须让车头顺着道路预期方向才能得分。
    *   如果车头偏离路线方向超过 45 度，直接判定为失控出界，结束回合。
    *   这迫使模型在接近弯道时必须**主动、提前打方向盘入弯**以获取分数，而不是被动等待横向偏移变大后再试图救车。
3.  **重置权重**：最终单步满分 1.0 被重新洗牌为：速度(0.2) + 轨迹居中(0.3) + 航向对齐(0.2) + 单步推进量(0.3)。

---

# RL 训练进阶调优与多场景泛化优化记录

本文档记录了在解决“多场景遗忘”、“弯道早切内径”以及“物理动作空间适配”等高阶问题时的调优经验。

## 1. 物理动作空间直接输出尝试与适配

**问题提出：**
为了提升过弯的果断性，尝试将 `action_space` 从归一化的 `[-1, 1]` 修改为物理量级 `[-7, 7]`（加速度）和 `[-0.7, 0.7]`（转向角），并移除 `tanh` 映射。

**观察与挑战：**
*   **KL 散度飙升**：网络权重的微小波动会被物理量级放大，导致 `approx_kl` 频繁突破 0.08，训练极度不稳定。
*   **控制精度失控**：探索阶段的随机噪声对方向盘维度过于剧烈，导致车辆频繁“自杀式”撞墙。

**适配对策（PPO 降温）：**
*   **极低学习率**：将 `learning_rate` 降至 `3e-5 ~ 5e-5`。
*   **强制熔断**：引入 `target_kl=0.015`，一旦更新步长过大立即中止该批次优化。
*   **收紧截断**：将 `clip_range` 从 0.2 缩减至 0.1，限制新老策略偏离度。

## 2. 训练策略重构：从顺序学习到洗牌轮询 (Shuffled Cycle)

**问题提出：**
采用顺序训练（练完直线再练弯道）会导致“灾难性遗忘”。模型在练直线时会废掉转弯能力；练弯道时又会产生起步就拐弯的“背板”行为，无法适配不同起点的场景。

**解决方案：**
1.  **废除顺序循环**：删除针对场景索引的外部 `for` 循环。
2.  **Episode 级洗牌重置**：在 `doReset` 逻辑中维护一个场景索引队列。每轮将所有场景索引打乱（Shuffle）后依次取出。
3.  **效果**：确保了所有场景在训练集中比例为 1:1:1，强迫模型提取“道路特征”而非记录“生存时间”，彻底解决了换起点即翻车的问题。

## 3. 观测视野优化：解决“贪婪预测”导致的早切弯问题

**问题提出：**
车辆在弯道末端会过早地向内侧偏移，试图提前对齐后续的直道。

**根本原因：**
**前瞻视野过远 (Over-foresight)**：原配置点位采样间隔 5m 且包含 5 个检查点，模型能“看”到 25 米外的路况。在弯道出口，模型为了获取前方直道的高额航向奖励，会贪婪地牺牲当前的轨迹精度提前切弯。

**调整方案：**
*   **缩短视野**：将采样间隔缩短至 3m，检查点减至 4 个。
*   **效果**：将前瞻视野限制在 12m 左右，强迫模型“老老实实”关注眼前的弯道几何特征，显著改善了出口处的路径贴合度。

## 4. 奖励函数鲁棒性增强

1.  **惩罚非线性化**：将居中奖励 `r_center` 从线性惩罚改为**平方惩罚** ($1 - (offset/max)^2$)。这使得车辆在接近车道边缘时受到的惩罚呈指数级增长，增强了模型保持在车道中心的“刚性”。
2.  **高熵探索防护**：将 `ent_coef` 提高至 `0.03 ~ 0.05`。在多场景混合训练初期，通过高熵奖励强制模型在不同场景下多做尝试，防止其过快收敛到某个场景的局部最优解。

---
*注：在探索过程中，曾尝试对底层 TESSNG 坐标系 (Y向南) 与 标准数学坐标系 (Y向北) 进行防腐层解耦重构，但考虑到引入的复杂度和潜在的边界污染风险，最终选择回退重构，保持在 TESSNG 原生坐标系上进行上述的最小化逻辑修补，确保了系统的稳定性。*
