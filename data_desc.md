

### 1. 配置参数

```python
# 配置参考
MAX_LANE_WIDTH = 4.0      # 最大车道宽度 (米)
MAX_SPEED = 33.3          # 最大车速 (约120km/h，单位 m/s)
MAX_STEER_ANGLE = 0.7     # 最大方向盘转角/前轮偏角 (弧度，约40度)
MAX_ACCEL = 5.0           # 最大纵向加速度 (m/s^2)
MAX_DECEL = -5.0          # 最大纵向减速度 (m/s^2)
MAX_YAW_RATE = 1.0        # 最大横摆角速度 (rad/s)
MAX_NAV_DIST = 100.0      # 导航点最大前瞻距离 (米)
MAX_CURVATURE_RAD = 1000.0 # 最大参考曲率半径 (米)
MAX_RADAR_DIST = 30.0     # 雷达射线最大探测距离 (米)
MAX_RELATIVE_SPEED = 40.0 # 周车最大相对速度差 (m/s)
```

**映射函数：**

*   **单极性数据（本身即为正数，如速度、距离）：** 
    直接除以最大值。$Norm = \max(0, \min(1, \frac{X}{X_{max}}))$
*   **双极性数据（有正有负，如转角、相对位置、相对速度）：** 
    先除以绝对值最大值（映射到 $[-1, 1]$），再平移缩放至 $[0, 1]$。$Norm = \max(0, \min(1, \frac{X}{2 \cdot X_{max}} + 0.5))$

---

### 2. 各维度状态量

#### 2.1 自车运动状态 (自车状态 * 5 + 运动学)
| 特征 | 物理意义/范围 | 归一化逻辑 | 说明 |
| :--- | :--- | :--- | :--- |
| **边界距离** | 距离道路左右边界的绝对距离 $[0, L_{width}]$ | `clip(dist / MAX_LANE_WIDTH, 0, 1)` | 若距离超限直接截断为1 |
| **车道偏移** | 自车相对于中心线的横向偏移 $[-L_{width}/2, L_{width}/2]$ | `clip((offset / MAX_LANE_WIDTH) + 0.5, 0, 1)` | 负值代表偏左，正值代表偏右，归一化后 0.5 为正中心。 |
| **角度差** | 航向角误差 $[-\pi, \pi]$ | `clip((angle_diff / math.pi + 1) / 2, 0, 1)` | 0代表朝向完全相反，0.5代表完全平行，1代表也是完全相反（另一侧） |
| **速度** | $[0, V_{max}]$ | `clip(v / MAX_SPEED, 0, 1)` | 倒车情况通常按绝对值或置0处理 |
| **转向角** | $[-\delta_{max}, \delta_{max}]$ | `clip((steer / (2 * MAX_STEER_ANGLE)) + 0.5, 0, 1)` | 左转与右转需对称映射，0.5 对应方向盘回正 |
| **加速度** | $[a_{min}, a_{max}]$ | `clip((acc - MIN_ACCEL) / (MAX_ACCEL - MIN_ACCEL), 0, 1)` | 因为最大加减速度往往不对称（刹车更猛），使用严格的 Min-Max 公式 |
| **角速度** | $[-\omega_{max}, \omega_{max}]$ | `clip((yaw_rate / (2 * MAX_YAW_RATE)) + 0.5, 0, 1)` | 0.5 为无横摆运动 |

#### 2.2 导航信息
| 特征 | 物理意义/范围 | 归一化逻辑 | 说明 |
| :--- | :--- | :--- | :--- |
| **前向/侧向差距** | checkpoint的局部 X/Y 坐标 | 前向: `clip(dx / MAX_NAV_DIST, 0, 1)`<br>侧向: `clip((dy / (2 * MAX_NAV_DIST)) + 0.5, 0, 1)` | X轴通常只取前方为正，Y轴分左右需平移到0.5 |
| **弯曲半径** | 曲率半径 $[0, R_{max}]$ | `clip(radius / MAX_CURVATURE_RAD, 0, 1)` | *注意：直道时曲率半径为无穷大，必须在代码中做异常捕获，将其强制置为 1.0* |
| **弯曲方向** | 原值：顺时针+1, 逆时针-1 | `(direction + 1) / 2` | 1.0 代表顺时针，0.0 代表逆时针，0.5 代表直道 (direction=0) |
| **车道角度差** | 始末角度差 $[-\pi, \pi]$ | `clip((lane_angle_diff / math.pi + 1) / 2, 0, 1)` | 同航向角误差逻辑 |

#### 2.3 周车交互信息
| 特征 | 物理意义/范围 | 归一化逻辑 | 说明 |
| :--- | :--- | :--- | :--- |
| **雷达点云 (72维)** | 碰撞距离 $[0, L_{ray\_max}]$ | `dist_to_collision / MAX_RADAR_DIST` | 根据表中描述，此项本身就是比例值。如果射线未击中任何物体，默认返回 1.0。 |
| **周车前向差距** | $\Delta X \in [-X_{max}, X_{max}]$ | `clip((delta_x / (2 * MAX_NAV_DIST)) + 0.5, 0, 1)` | 因为其他车/目标可能在自车后方，$\Delta X$ 可能为负，故需平移到 0.5 中心 |
| **周车侧向差距** | $\Delta Y \in [-Y_{max}, Y_{max}]$ | `clip((delta_y / (2 * MAX_LANE_WIDTH)) + 0.5, 0, 1)` | - |
| **周车纵/横向速度差** | $\Delta V_x, \Delta V_y \in [-V_{max}, V_{max}]$ | `clip((delta_vx / (2 * MAX_RELATIVE_SPEED)) + 0.5, 0, 1)` | 0.5 代表相对静止；>0.5 代表相对自车在远离/更快，<0.5 代表相对自车在靠近/更慢。 |

---
